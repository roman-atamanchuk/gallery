package util;

/**
 * Placeholder utility class for shared application constants.
 * Populate this later only when cross-cutting constants are actually needed.
 */
public final class AppConstants {

    private AppConstants() {
        // Prevent instantiation of utility classes.
    }
}
